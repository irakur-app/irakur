<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% for (var i = 0; i < links.length; i++) { %>
        <a href="<%= links[i].url %>"><%= links[i].name %></a><br>
    <% } %>
  </body>
</html>